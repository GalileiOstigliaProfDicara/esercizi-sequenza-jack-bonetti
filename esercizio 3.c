#include <stdio.h>

int main() {
    int base, altezza, area, perimetro;

    printf("inserisci la base e l'altezza del rettangolo:");
    scanf("%d", &altezza);
    scanf("%d", &base);
    area = base * altezza;

    printf("l'area di %d è %d\n", rettangolo, area);

    perimetro = base *2 + altezza*2;
    printf("il perimetro di %d è %d\n, rettangolo, perimetro");

    return 0;
 





}